name: CI

on:
  push: ~
  pull_request: ~
  schedule:
    - cron:  '0 12 * * *'

jobs:
  build_and_test:
    strategy:
      fail-fast: false
      matrix:
        node-version: [14.x]
        include:
          - scenario: "all 50"
          - scenario: "all 250"
          - scenario: "all 500"
          - scenario: "all 2500"
          - scenario: "all 5000"
          - scenario: "all 10000"

          - scenario: "uniform 5000 100"
          - scenario: "uniform 5000 200"
          - scenario: "uniform 5000 250"
          - scenario: "uniform 5000 500"
          - scenario: "uniform 5000 1000"

          - scenario: "random 5000 5"
          - scenario: "random 5000 10"
          - scenario: "random 5000 20"
          - scenario: "random 5000 40"
          - scenario: "random 5000 80"

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Install dependencies
      run: npm install
    - name: Create scenario
      run: ts-node run.ts ${{ matrix.scenario }}
    - name: Build & Test
      run: npm run build && npm run mutate
